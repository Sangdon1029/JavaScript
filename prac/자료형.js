const name ="Mike";
const age = 30;
// 문자는 아래 3가지로 표현가능합니다
const name1 = "Mike";
const name2 = 'Mike';
const name3 = `Mike`;
// 작은 따음표만 사용할 경우
const message2 = 'I\'m a boy.';
// 백틱은 문자열 내부에 변수를 입력할 때 편합니다
const message3 = `My name is ${name}`;

// --숫자형
// 만약 숫자를 /0으로 하면 무한대 infinite
// 만약 문자열 string을 숫자로 나누게 되면 NaN으로 나오게묌

// --boolean
// true = 1 , false = 0;

// null undefined 

// typeof 연산자는 변수의 자료형을 알아낼 수 있습니다

// typeof null = object라고 나오지만
// 사실 null은 객체가 아닙니다. 
// 하위호환성을 유지하기 위해서 수정하지 않습니다

